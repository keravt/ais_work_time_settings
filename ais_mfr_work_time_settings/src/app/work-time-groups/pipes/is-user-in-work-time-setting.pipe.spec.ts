import { IsUserInWorkTimeSettingPipe } from './is-user-in-work-time-setting.pipe';

describe('IsUserInWorkTimeSettingPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUserInWorkTimeSettingPipe();
    expect(pipe).toBeTruthy();
  });
});
