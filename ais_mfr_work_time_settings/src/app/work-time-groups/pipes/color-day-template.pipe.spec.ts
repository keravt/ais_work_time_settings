import { ColorDayTemplatePipe } from './color-day-template.pipe';

describe('ColorDayTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorDayTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
