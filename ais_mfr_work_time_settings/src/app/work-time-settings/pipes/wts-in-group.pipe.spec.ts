import { WtsInGroupPipe } from './wts-in-group.pipe';

describe('WtsInGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new WtsInGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
