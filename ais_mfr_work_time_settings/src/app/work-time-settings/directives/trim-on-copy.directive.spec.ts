import { TrimOnCopyDirective } from './trim-on-copy.directive';

describe('TrimOnCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimOnCopyDirective();
    expect(directive).toBeTruthy();
  });
});
